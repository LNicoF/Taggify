name: taggify
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  uuid: any

dev_dependencies:
  flutter_lints: ^2.0.0
  test: ^1.24.7

flutter:
  uses-material-design: true
